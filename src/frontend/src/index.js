import React from 'react';
import ReactDOM from 'react-dom/client';        
import ChannelGate from './ChannelSelect/ChannelGate'

const root = ReactDOM.createRoot(document.getElementById('root'));
//Todo: Create a flash welcome page describing how to do shit
// Then click ok on that page to get to the real app
root.render(<ChannelGate />);
